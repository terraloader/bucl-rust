# maxlength — return the character count of the longest argument.
#
# Arguments: one or more strings.
# Returns: length of the longest one.
#
# Usage:
#   {m} maxlength "ha" "llo"   # {m} = "3"

{_max} = "0"

{r} repeat {argc}
	{_i} math "{r/index}-1"
	{_item} = {args/{_i}}
	{_len} = {_item/length}
	if {_len} > {_max}
		{_max} = {_len}

{return} = {_max}
